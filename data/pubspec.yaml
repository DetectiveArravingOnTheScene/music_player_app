name: data
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
    sdk: ^3.10.1

resolution: workspace

dependencies:
    isar_plus: ^1.1.8
    isar_plus_flutter_libs: ^1.1.8
    path_provider: ^2.1.5

dev_dependencies:
    build_runner: ^2.10.4
    lints: ^6.0.0
    test: ^1.25.6
